name: CI

on:
  push:
  pull_request:

jobs:
  shell-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run resolve-proton-wine tests
        run: bash tests/test-resolve-proton-wine.sh
